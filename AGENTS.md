# 项目接手规则

完整阅读 `docs/MASTER_PLAN.md` 和 `docs/FIRST_DELIVERY.md`，当前规划仅此两份。总纲定义产品边界，首交付计划定义当前范围和状态；两者冲突时先澄清并统一，不能从归档找旧规则覆盖。禁止继续新增平行TODO、ROADMAP或设计版本补丁。

`docs/archive/` 全部是失效历史，仅在定位旧实现/测试证据时查阅；其内部链接保留历史路径，可能不再有效。`docs/testing/`、`docs/verification/` 与环境记录不是产品指令。

改动前检查实际源码、Git和产物，不凭历史记录假定通过。保留用户未提交内容。玩法不与外观硬编码绑定，多人从首批接入。UI和正式美术先出集中提案给用户确认，必要的玩法反馈不拖后。

按批构建和定向测试；仅必要的输入/镜头/视觉检查使用Computer Use。文档任务不启动UE，不授权购买、安装、上传、提交或推送。自动测试不证明有趣；真人试玩未发生就明确未验证。

执行状态与新证据索引只更新 `docs/FIRST_DELIVERY.md`。本地提交不等于推送，构建成功不等于实机通过，局域网不等于公网。模型下载不等于可交付资产。
